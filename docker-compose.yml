# Docker Compose — Development Environment
# Configured during project planning. Services below are placeholders.

version: "3.8"

services:
  # backend:
  #   build: ./backend
  #   ports:
  #     - "${APP_PORT:-3000}:3000"
  #   env_file: .env
  #   depends_on:
  #     - db

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "5173:5173"

  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: app
  #     POSTGRES_USER: app
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   db_data:
